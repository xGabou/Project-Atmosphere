# Remaining Files Handling Matrix

| File | Classification | Reason | Action taken | If unchanged, why unchanged is acceptable | If risky, what exact risk blocks automatic refactor | Future action if any |
|---|---|---|---|---|---|---|
| `src/main/java/net/Gabou/projectatmosphere/api/AtmoApi.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/api/AtmosphereWorldEffect.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/api/common/cloud/region/ITornadoRegion.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/api/common/cloud/region/ScAPICloudRegionTornadoAccess.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/api/common/cloud/region/TornadoDescriptor.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/api/common/event/CloudRegionTickEventTornadoAccess.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/api/CropStressEvent.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/api/CropStressType.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/api/event/AtmosphereWeatherTickEvent.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/api/ForecastSampling.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/api/WeatherSnapshot.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/api/WindVectorApi.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/async/BiomeSampler.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/async/PoolType.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/async/ThreadingDetector.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/auth/ClientAuth.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/auth/ClientLauncherGuards.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/auth/PendingAuthManager.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/auth/ServerAuth.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/auth/SharedSecret.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/auth/TLauncherDetectedHandler.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/AnemometerBlock.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/BarometreBlock.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/BlockManager.java` | TRUE_HANDLED_RESTRUCTURED | Extracted weather-debris budget handling into a helper and cleaned the spawn orchestration flow. | Delegated token budgeting to `WeatherDebrisBudget`. | N/A; this file was structurally improved. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/DustLayerBlock.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/HumidimeterBlock.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/InstrumentBlock.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/InstrumentReader.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/SandLayerBlock.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/StormShieldBlock.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/StormSirenBlock.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/ThermometerBlock.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/blocks/WeatherVaneBlock.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/atmosphere/AtmosphereClientState.java` | TRUE_HANDLED_RESTRUCTURED | Internal target-update logic was extracted into small helpers while keeping tick order and smoothing behavior intact. | Helper extraction inside the class. | Not applicable; the class was safely restructured. |  | No further action unless a larger client-state split is planned. |
| `src/main/java/net/Gabou/projectatmosphere/client/BiomeClientTemperatureCache.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/ClientPacketHandlers.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/ClientSyncLock.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/crash/ProjectAtmosphereCrashHandler.java` | TRUE_HANDLED_GOOD_AS_IS | Broad but already well-structured crash capture and presentation helper; no safe structural gain justified. | Left unchanged in this pass. | The method groups already mirror the crash capture flow and the helper surface is self-contained. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/fog/AtmosphereFogState.java` | TRUE_HANDLED_RESTRUCTURED | Internal target-update logic was extracted into a small helper while keeping fog behavior and debug override semantics intact. | Helper extraction inside the class. | Not applicable; the class was safely restructured. |  | No further action unless a larger client-fog split is planned. |
| `src/main/java/net/Gabou/projectatmosphere/client/fog/FogBiomeClassifier.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/HUDOverlayRenderer.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/hurricane/cache/ClientHurricaneStateCache.java` | TRUE_HANDLED_MOVED | Moved to a clearer cache-specific package. | Package declaration and call sites updated. |  |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/loading/ClientForecastLoadingLifecycle.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/loading/ClientForecastLoadingWorkQueue.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/loading/ForecastLoadingOverlayRenderer.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/loading/ForecastLoadingStage.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/loading/ForecastLoadingState.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/loading/IntegratedForecastLoadingBridge.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/shader/HurricaneShaders.java` | TRUE_HANDLED_MOVED | Moved to a shader-specific package. | Package declaration and call sites updated. |  |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/SideInfo.java` | TRUE_HANDLED_MOVED | Moved out of the render package into `client/render/mesh` during the risky-files cluster restructure. | Package ownership was improved without changing behavior. | Not applicable; the old path no longer exists. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/pipeline/SimpleCloudsDhPipelineSelector.java` | TRUE_HANDLED_MOVED | Moved to a pipeline-specific package. | Package declaration and call sites updated. |  |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/SimpleCloudsHurricaneRenderer.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/sky/SkyEffectState.java` | TRUE_HANDLED_MOVED | Moved to a sky-effect-specific package. | Package declaration and call sites updated. |  |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/shader/TornadoShaders.java` | TRUE_HANDLED_MOVED | Moved to a shader-specific package. | Package declaration and call sites updated. |  |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/VolumeBoxMesh.java` | TRUE_HANDLED_MOVED | Moved out of the render package into `client/render/mesh` during the risky-files cluster restructure. | Package ownership was improved without changing behavior. | Not applicable; the old path no longer exists. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/screen/ProjectAtmosphereCrashScreen.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/screen/WeatherRadarScreen.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/sound/TornadoAudioClient.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/sound/TornadoRoarLoop.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/ClientSystemProfile.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/clouds/CloudDebugRenderer.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/clouds/CloudRenderSnapshot.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/clouds/CloudRenderStateCache.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/command/CloudDumpCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/command/DebugAtmoCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/command/SpawnCloudCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/command/TelemetryDebugClientCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/auroras/AuroraCompatController.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/auroras/AuroraSeasonHelper.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/ColdSweatCompat.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/CompatHandler.java` | TRUE_HANDLED_RESTRUCTURED | Split module detection and init logging into package-private helpers. | Delegated module detection to `CompatModuleDetector` and logging to `CompatInitLogger`. | N/A; this file was structurally improved. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/LegendarySurvivalCompat.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/rainbows/RainbowWeatherTracker.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/sky/AtmosphereSkyEffectController.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/sky/AtmosphereSkySample.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/sky/AtmosphereSkySampler.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/sky/SkyConditionMath.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/temperature/ClientTemperatureResolver.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/TemperatureMod.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/ToughAsNailsCompat.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/config/AtmoCommonConfig.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/config/AtmoConfigScreen.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/data/TornadoStorageManager.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/event/BiomeChangeManager.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/event/EclipticTracker.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/event/EventHandler.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/event/SimpleCloudsEventListener.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/event/TemperatureTickHandler.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/items/Anemometer.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/items/Balai.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/items/Barometre.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/items/CloudProbeItem.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/items/Humidimeter.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/items/InstrumentBlockItem.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/items/Thermometre.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/items/WeatherRadarItem.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/manager/AtmosphereWorldEffectsManager.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/manager/CropStressManager.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/manager/ForecastDataStorage.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/manager/ForecastGenerator.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/manager/ForecastOrchestrator.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/manager/SandStormManager.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/BiomeFreezingMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/CachedTemperatureUtilMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/BindingManagerAccessor.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/DefaultPipelineHurricaneMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/DefaultPipelineTornadoMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/LoadingOverlayMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/LoadingScreenMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/MinecraftCrashHandlerMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/particle/ParticleMotionAccessor.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/particle/WindBentParticleEngineMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/ShaderSupportPipelineHurricaneMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/ShaderSupportPipelineTornadoMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/SimpleCloudsRendererLightningBufferMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/CloudGeneratorHurricaneReservationMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/CloudMeshGeneratorAccessor.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/CloudMeshGeneratorDiagnosticsAccessor.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/CloudRegionMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/CloudRegionTickEventMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/compat/auroras/AuroraRendererMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/compat/rainbows/RainbowsRendererParticleMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/InfoMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/MixinSandstormDebugBlocker.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/MultiRegionCloudMeshGeneratorMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/OverwriteDesertSound.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/PAMixinPlugin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/SeasonHooksMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/ServerLevelSnowStormMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/SimpleCloudsCloudManagerMixin.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphereStatusSyncManager.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericStateRegistry.java` | TRUE_HANDLED_RESTRUCTURED | Split legacy lookup and region-radius helpers into a dedicated package-private collaborator. | Delegated lookup/index/radius helpers to `AtmosphericStateLookup`. | N/A; this file was structurally improved. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericUpdateScheduler.java` | TRUE_HANDLED_RESTRUCTURED | Split telemetry emission into a dedicated reporter and kept the update cadence intact. | Delegated telemetry emission to `AtmosphericTelemetryReporter`. | N/A; this file was structurally improved. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/CloudWaterExchange.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/CloudWaterService.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/CycloneManager.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/CycloneSnapshot.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/HumidityBudget.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/HumidityBudgetService.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/HumiditySourceProfile.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/RainSystem.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/RegionAtmosphereState.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/SunlightController.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/core/BiomeForecast.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/core/CloudLibrary.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/core/ForecastType.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/core/WindVector.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/fog/FogCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/fog/FogHeuristics.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/humidity/HumidityCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/humidity/HumidityGenerator.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneBlockBreakRules.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneCategory.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneCloudVolume.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneDestructionManager.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneInstance.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneManager.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneRenderDescriptor.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneRenderSnapshot.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneSemantics.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneSemanticSample.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneSnapshot.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneWindField.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/ocean/AtmosphericVolume.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/ocean/AtmosVolumeInfluence.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/ocean/influence/AtmosphereFluxInfluence.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/ocean/influence/BasinPressureMemoryInfluence.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/ocean/influence/BasinThermalMemoryInfluence.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/ocean/OceanBasin.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/ocean/OceanBasinManager.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/ocean/OceanBiomeClassifier.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/ocean/OceanInfluence.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/ocean/OceanUpdateContext.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/pressure/PressureCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/pressure/PressureCurveGenerator.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/pressure/PressureGenerator.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/BiomeFallbackSnapshot.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/BiomeForecastGenerator.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/BiomeForecastSnapshot.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/DefaultRegionCurves.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/FileRegionPersistence.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/ForecastRegion.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/ForecastRegionId.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/GridRegionIndex.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/LegacyBiomeForecastGenerator.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/RegionAdapters.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/RegionCurves.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/RegionForecastOrchestrator.java` | TRUE_HANDLED_RESTRUCTURED | Split corruption detection into a dedicated validator helper. | Delegated corruption checks to `RegionForecastCorruptionValidator`. | N/A; this file was structurally improved. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/RegionIdCodec.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/RegionIndex.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/RegionOrchestratorBootstrap.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/RegionPersistence.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/WeightedCurve.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/region/WeightedWindCurve.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/sandStorm/SandStormAPI.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/LeafState.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/PaClimateVigorProvider.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/SeasonalTreesCore.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/SeasonalTreesPaSeasonProvider.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/SeasonalTreesSeasonProvider.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/SeasonalTreesVigorProvider.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/SeasonPhase.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/SeedPayload.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/TreeKey.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/TreeRecord.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/TreeState.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/core/TreeType.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/data/SeasonalTreesSavedData.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/integration/DynamicTreesAccessor.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/integration/DynamicTreesDormancyHelper.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/integration/SeasonalTreesAccessorRegistry.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/integration/SeasonalTreesTreeAccessor.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/integration/VanillaTreesAccessor.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/SeasonalTreesBootstrap.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/SeasonalTreesEventHandler.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/transport/LocalSeedTransport.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/transport/SeasonalTreesSeedTransport.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/seasonaltrees/transport/WindSeedTransport.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/snowstorm/SnowStorm.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/snowstorm/SnowstormManager.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/storm/GlobalStormHistoryData.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/storm/StormChanceAdjuster.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/storm/StormGenerator.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/storm/StormLullHook.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/command/TemperatureCommandHelper.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/command/TemperatureCommands.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/config/BiomeTempConfig.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/config/BiomeTempUserConfig.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/core/SpikeProvider.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/core/TemperatureProvider.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/spike/commands/ApplyOngoingSpikeCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/spike/commands/ApplyRandomJoltCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/spike/commands/SpikeCommands.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/spike/commands/StartNewSpikeCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/spike/SpikeData.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/spike/SpikeManager.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/spike/SpikeState.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/spike/SpikeStateStorage.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/spike/SpikeType.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/util/TemperatureGenerator.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/util/TemperatureUtils.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/temperature/variation/VariationGenerator.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/GlassDamageManager.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoInstance.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoLevel.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoManager.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoProbabilityManager.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoSnapshot.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoSpawner.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoSpawnScheduler.java` | TRUE_HANDLED_MOVED | Moved into a clearer tornado scheduling package. | Moved to `modules/tornado/scheduling`. | N/A; this file was structurally improved. | None for the applied move. | Scheduler ownership is now clearer. |
| `src/main/java/net/Gabou/projectatmosphere/modules/weather/RegionalWeatherPhase.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/weather/ServerWeatherStateResolver.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/weather/StormLifecyclePhase.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |

## 023 Cluster Update: Tornado Lifecycle

| File | Classification | Reason | Action taken | If unchanged, why unchanged is acceptable | If risky, what exact risk blocks automatic refactor | Future action if any |
| --- | --- | --- | --- | --- | --- | --- |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoManager.java` | STILL_RISKY_MANUAL_REVIEW | Owns spawn, sync, tick, persistence, and client/server tornado lists. | Left unchanged. | Lifecycle flow remains readable enough to avoid a noisy refactor. | Spawn/sync/persistence/tick behavior is tightly coupled. | Future helper extraction only if lifecycle seams become obvious. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoInstance.java` | STILL_RISKY_MANUAL_REVIEW | Large per-tornado lifecycle/state machine with render snapshot and destruction logic. | Left unchanged. | It is already segmented into lifecycle and debug sections. | Physics, destruction, persistence, and snapshot behavior are too sensitive. | Future method grouping only if a safe seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoLevel.java` | TRUE_HANDLED_GOOD_AS_IS | Small severity enum already clean enough. | Left unchanged. | It is a compact data model with no safe structural gain. | Level thresholds and damage values are behavior-sensitive. | Leave as-is. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoProbabilityManager.java` | STILL_RISKY_MANUAL_REVIEW | Computes tornado risk and coordinates scheduled tornado spawning. | Left unchanged. | The current flow is understandable and linked to spawn timing. | Risk math and spawn cadence are too sensitive for automatic reshaping. | Future split of risk computation vs spawn trigger only if behavior remains identical. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoSpawner.java` | STILL_RISKY_MANUAL_REVIEW | Owns spawn positioning and spawn application helpers. | Left unchanged. | The spawn flow is already localized enough to avoid a noisy diff. | Spawn selection and spawn output are behavior-sensitive. | Future helper extraction only if the same output is preserved. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoSpawnScheduler.java` | TRUE_HANDLED_MOVED | Moved into a clearer scheduling package. | Moved to `modules/tornado/scheduling`. | N/A; this file was structurally improved. | None for the applied move. | Scheduler ownership is now clearer; leave the remaining tornado classes for manual review. |

## Counts
- TRUE_HANDLED_GOOD_AS_IS: 257
- TRUE_HANDLED_RESTRUCTURED: 3
- TRUE_HANDLED_MOVED: 3
- TRUE_HANDLED_RENAMED: 0
- TRUE_HANDLED_SPLIT: 0
- TRUE_HANDLED_MERGED: 0
- TRUE_HANDLED_EMPTY_DELETED: 0
- TRUE_HANDLED_FACADE_KEPT: 0
- TRUE_HANDLED_RISKY_MANUAL_REVIEW: 73

Note: the moved source file was removed from the remaining-file set via the handled registry update, so it is tracked under the source-change summary rather than the remaining-file classification counts.

## 023 Cluster Update: Tornado Lifecycle

| File | Classification | Reason | Action taken | If unchanged, why unchanged is acceptable | If risky, what exact risk blocks automatic refactor | Future action if any |
| --- | --- | --- | --- | --- | --- | --- |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoManager.java` | STILL_RISKY_MANUAL_REVIEW | Owns spawn, sync, tick, persistence, and client/server tornado lists. | Left unchanged. | Behavior-sensitive orchestration; lifecycle order remains readable enough for now. | Spawn/sync/persistence/tick behavior is tightly coupled. | Future helper extraction only if lifecycle seams become obvious. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoInstance.java` | STILL_RISKY_MANUAL_REVIEW | Large per-tornado lifecycle/state machine with render snapshot and destruction logic. | Left unchanged. | The class is already segmented into lifecycle and debug sections. | Physics, destruction, persistence, and snapshot behavior are too sensitive. | Future method grouping only if a safe seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoLevel.java` | TRUE_HANDLED_GOOD_AS_IS | Small severity enum already clean enough. | Left unchanged. | It is a compact data model with no safe structural gain. | Level thresholds and damage values are behavior-sensitive. | Leave as-is. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoProbabilityManager.java` | STILL_RISKY_MANUAL_REVIEW | Computes tornado risk and coordinates scheduled tornado spawning. | Left unchanged. | The current flow is understandable and linked to spawn timing. | Risk math and spawn cadence are too sensitive for automatic reshaping. | Future split of risk computation vs spawn trigger only if behavior remains identical. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoSpawner.java` | STILL_RISKY_MANUAL_REVIEW | Owns spawn positioning and spawn application helpers. | Left unchanged. | The spawn flow is already localized enough to avoid a noisy diff. | Spawn selection and spawn output are behavior-sensitive. | Future helper extraction only if the same output is preserved. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoSpawnScheduler.java` | TRUE_HANDLED_MOVED | Moved into a clearer scheduling package. | Moved to `modules/tornado/scheduling`. | N/A; this file was structurally improved. | None for the applied move. | Scheduler ownership is now clearer; leave the remaining tornado classes for manual review. |
| `src/main/java/net/Gabou/projectatmosphere/modules/weather/StormMotionModel.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/weather/StormSeverityScale.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/weather/StormShieldManager.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/FloatRange.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/HighWindModel.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/LowWindModel.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/RegionWindForecastApi.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/TornadoWindModel.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/WindCommand.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/WindConfig.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/WindEngine.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/WindForces.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/WindForecast.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/WindForecastApi.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/WindForecastPart.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/WindGenerator.java` | TRUE_HANDLED_RISKY_MANUAL_REVIEW | Previously audited as broad, behavior-sensitive, or mixin/render-path code. | Left unchanged in this pass. | Safe automatic restructuring would be noisy or could disturb runtime/render/mixin behavior. | Behavior-sensitive surface; package/method reordering could obscure lifecycle or injection flow. | Manual review before any deeper refactor. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/WindMath.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/wind/WindRuntimeState.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/AuthChallengePacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/AuthChallengeReplyPacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/BiomeDayTemperaturePacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/FogDebugOverridePacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/ForecastLoadingStatusPacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/InstrumentReadoutPacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/NetworkHandler.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/RemoveTornadoPacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/SpawnTornadoPacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/SyncAtmosphereStatusPacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/SyncHurricaneStatePacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/SyncTornadoesPacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/network/SyncWindPacket.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/particles/DebrisParticle.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/particles/DebrisParticleData.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/particles/WindLeafParticle.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/particles/WindParticlePusher.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/particles/WindStreakParticle.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/ProjectAtmosphere.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/registry/ClientOnlyRegistrar.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/registry/ModBlocks.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/registry/ModClient.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/registry/ModItems.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/registry/ModParticles.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/registry/ModSounds.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/registry/ModTabs.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/seasons/EclipticSeasonsSeasonDelegate.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/seasons/SeasonBootstrap.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/seasons/SeasonProvider.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/seasons/SeasonProviderRegistry.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/seasons/SeasonSnapshot.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/seasons/SeasonStage.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/seasons/SeasonTimeDelegate.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/seasons/SeasonTimeHelper.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/seasons/SereneSeasonsEventBridge.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/seasons/SereneSeasonsSeasonDelegate.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/ServerSystemProfile.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/telemetry/ServerTelemetrySampler.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/telemetry/TelemetryCollector.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/telemetry/TelemetryExportService.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/telemetry/TelemetryModels.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/telemetry/TelemetryRingBuffer.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/tools/debug/ShaderSnapshot.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/tools/FlowMapGenerator.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/AsyncAtmosphereService.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/AtmosphereUtils.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/AtmosphericPhysics.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/BiomeInstanceKey.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/DelayedTaskScheduler.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/HumidityGuard.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/HurricaneUpload.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/ICloudRegionId.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/InstrumentUtils.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/RegionInstanceKey.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/RegionUpload.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/StorageUtils.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/TickCounter.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/TornadoUpload.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/util/UnitFormatter.java` | TRUE_HANDLED_GOOD_AS_IS | Previously audited as stable and sufficiently organized for now. | Left unchanged in this pass. | No safe architectural gain was justified without creating a noisy or risky diff. |  | None. |

## Counts
- TRUE_HANDLED_GOOD_AS_IS: 256
- TRUE_HANDLED_RESTRUCTURED: 7
- TRUE_HANDLED_MOVED: 7
- TRUE_HANDLED_RENAMED: 0
- TRUE_HANDLED_SPLIT: 0
- TRUE_HANDLED_MERGED: 0
- TRUE_HANDLED_EMPTY_DELETED: 0
- TRUE_HANDLED_FACADE_KEPT: 0
- TRUE_HANDLED_RISKY_MANUAL_REVIEW: 63

Note: the moved and deleted source files from this pass were removed from the remaining-file set via the handled registry update, so they are tracked under the source-change summary rather than the remaining-file classification counts.

## 021 Cluster Update: Broad Manager or Orchestration Risk

| File | Classification | Reason | Action taken | If unchanged, why unchanged is acceptable | If risky, what exact risk blocks automatic refactor | Future action if any |
| --- | --- | --- | --- | --- | --- | --- |
| `src/main/java/net/Gabou/projectatmosphere/manager/AtmosphereManager.java` | STILL_RISKY_MANUAL_REVIEW | Central server lifecycle coordinator with player login, tick, regeneration, season, and cloud queue responsibilities. | Left unchanged in this pass. | This class is broad but the lifecycle flow is still readable enough to avoid a noisy refactor. | Tick/order/sync/cloud queue behavior is too tightly coupled for automatic restructuring. | Manual split candidate for future lifecycle helper extraction. |
| `src/main/java/net/Gabou/projectatmosphere/manager/ForecastOrchestrator.java` | TRUE_HANDLED_GOOD_AS_IS | Stable forecast bootstrap/orchestration surface; no safe structural gain was justified beyond comments. | Left unchanged in this pass. | It is broad, but the current method ordering already mirrors its lifecycle flow. | Any deeper split risks touching forecast bootstrap and regeneration semantics. | Revisit only if a real split boundary becomes obvious. |

## 022 Cluster Update: Atmosphere Runtime

| File | Classification | Reason | Action taken | If unchanged, why unchanged is acceptable | If risky, what exact risk blocks automatic refactor | Future action if any |
| --- | --- | --- | --- | --- | --- | --- |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphereStatusSyncManager.java` | TRUE_HANDLED_GOOD_AS_IS | Narrow sync coordinator already clean enough. | Left unchanged. | Packet flow is compact and clear. | Sync behavior and packet contents are too sensitive for opportunistic refactor. | Leave as-is unless sync surface grows. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericStateRegistry.java` | STILL_RISKY_MANUAL_REVIEW | Central registry for region state storage, legacy lookup, and active-state tracking. | Left unchanged. | The current layout is serviceable and still readable. | State resolution and active-set semantics are too central to change blindly. | Future split into storage vs lookup helpers if needed. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericUpdateScheduler.java` | STILL_RISKY_MANUAL_REVIEW | Schedules active/passive updates and applies telemetry-heavy deltas. | Left unchanged. | Method order already reflects the runtime lifecycle. | Changing cadence or delta math would risk subtle runtime regressions. | Future snapshot/delta/application split only if a clear seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/CycloneManager.java` | STILL_RISKY_MANUAL_REVIEW | Owns cyclone lifecycle, snapshots, and direct atmosphere mutation. | Left unchanged. | It remains too tightly coupled to cyclone motion and update flow. | Behavior-sensitive lifecycle, snapshot, and drift logic. | Future helper extraction only if a safe seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/RegionAtmosphereState.java` | STILL_RISKY_MANUAL_REVIEW | Holds live atmospheric values plus legacy compatibility accessors. | Left unchanged. | Legacy section is already isolated and readable. | Interpolation/clamp/snapshot semantics are sensitive. | Future compatibility-helper split only if behavior stays identical. |
| `src/main/java/net/Gabou/projectatmosphere/manager/AtmosphereManager.java` | TRUE_HANDLED_RESTRUCTURED | Extracted cloud-region tracking into a collaborator. | Delegated cloud-region bookkeeping to `AtmosphereCloudRegionTracker`. | N/A; the file was structurally improved. | None for the applied extraction. | Future split could separate sync, regen, and season transition helpers. |

## 022 Cluster Update: Atmosphere Runtime Risk

| File | Classification | Reason | Action taken | If unchanged, why unchanged is acceptable | If risky, what exact risk blocks automatic refactor | Future action if any |
| --- | --- | --- | --- | --- | --- | --- |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphereStatusSyncManager.java` | TRUE_HANDLED_GOOD_AS_IS | Narrow sync coordinator already clean enough; no safe structural gain justified. | Left unchanged. | Packet flow is already compact and readable. | Sync behavior and packet contents are too sensitive for automatic restructuring. | Leave for manual review only if sync surface expands. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericStateRegistry.java` | STILL_RISKY_MANUAL_REVIEW | Central registry with state storage, legacy lookup, and active-state tracking. | Left unchanged. | The current structure is still readable enough for runtime state access. | State resolution and active-set semantics are too central to reshape blindly. | Future split of storage vs lookup if a stronger boundary appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericUpdateScheduler.java` | STILL_RISKY_MANUAL_REVIEW | Schedules active/passive updates and applies telemetry-heavy deltas. | Left unchanged. | Method grouping mirrors update lifecycle already. | Changing update cadence or delta math would risk subtle runtime regressions. | Future split of snapshot/delta/application helpers only if needed. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/CycloneManager.java` | STILL_RISKY_MANUAL_REVIEW | Owns cyclone lifecycle, snapshots, and direct atmosphere mutation. | Left unchanged. | The class remains too tightly coupled to cyclone motion and update flow. | Behavior-sensitive lifecycle, snapshot, and drift logic. | Future helper extraction only if a safe seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/RegionAtmosphereState.java` | STILL_RISKY_MANUAL_REVIEW | Holds live atmospheric values plus legacy compatibility accessors. | Left unchanged. | It is already logically grouped and the legacy section is isolated. | Changing interpolation/clamping or snapshot semantics would be risky. | Future split of legacy compatibility helpers only if behavior remains identical. |
| `src/main/java/net/Gabou/projectatmosphere/manager/AtmosphereManager.java` | TRUE_HANDLED_RESTRUCTURED | Extracted cloud-region tracking into a collaborator, reducing lifecycle clutter. | Delegated cloud-region bookkeeping to `AtmosphereCloudRegionTracker`. | N/A; this file was structurally improved. | None for the applied extraction. | Further split could separate sync, regeneration, and season transition helpers later. |
| `src/main/java/net/Gabou/projectatmosphere/manager/ForecastGenerator.java` | TRUE_HANDLED_GOOD_AS_IS | Stable forecast generation pipeline; no safe structural gain was justified beyond comments. | Left unchanged in this pass. | The class is large but the generation phases are already grouped coherently. | Forecast math and save/hydration behavior are too sensitive for opportunistic refactoring. | Leave alone until a stronger split boundary is identified. |
| `src/main/java/net/Gabou/projectatmosphere/manager/SimpleCloudSpawner.java` | TRUE_HANDLED_RESTRUCTURED | Extracted cloud spawn severity rules into a dedicated helper. | Moved severity math into `CloudSpawnSeverityRules` and updated call sites. | N/A; this file was structurally improved. | None for the applied extraction. | Future work can split spawn application from policy selection further if needed. |
| `src/main/java/net/Gabou/projectatmosphere/compat/SimpleCloudsCompat.java` | TRUE_HANDLED_RESTRUCTURED | Redirected cloud severity math to the shared helper and kept compatibility behavior intact. | Updated call sites to the new helper. | N/A; this file was structurally improved. | None for the applied call-site update. | Future helper extraction could narrow region creation policy later. |
| `src/main/java/net/Gabou/projectatmosphere/compat/CompatHandler.java` | STILL_RISKY_MANUAL_REVIEW | Central compatibility detection and initialization logging surface. | Left unchanged in this pass. | Module detection is readable enough, but the class is still too central for automatic splitting. | Changing compat detection or init order could alter startup behavior. | Manual split candidate if compat boot policy grows further. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/CloudManager.java` | TRUE_HANDLED_RESTRUCTURED | Extracted shared cloud spawn severity rules and removed the private duplicate selector. | Updated imports and call sites to `CloudSpawnSeverityRules`; removed the duplicate selector method. | N/A; this file was structurally improved. | None for the applied extraction. | Future work can separate telemetry, spawn application, and region sampling if needed. |

## 026 Runtime Lifecycle Manager Resolution

| File | Classification | Reason | Action taken | If unchanged, why unchanged is acceptable | If risky, what exact risk blocks automatic refactor | Future action if any |
| --- | --- | --- | --- | --- | --- | --- |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/CycloneManager.java` | TRUE_HANDLED_RESTRUCTURED | Split cyclone delta application into a package-private collaborator. | Delegated cyclone delta application to `CycloneImpactApplier`. | N/A; this file was structurally improved. | None for the applied extraction. | Further helper extraction only if a safe seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoManager.java` | TRUE_HANDLED_RESTRUCTURED | Split client snapshot logging into a package-private collaborator. | Delegated client snapshot logging to `TornadoClientSnapshotLogger`. | N/A; this file was structurally improved. | None for the applied extraction. | Could still separate sync packet building later if needed. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoInstance.java` | TRUE_HANDLED_GOOD_AS_IS | Broad, behavior-sensitive per-tornado lifecycle and snapshot class with no safe seam for this pass. | Left unchanged. | The current layout already mirrors lifecycle and debug flow well enough. | Physics, destruction, persistence, and snapshot behavior are too sensitive. | Manual review only if a narrow helper seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneManager.java` | TRUE_HANDLED_RESTRUCTURED | Split cyclone environment analysis into a package-private collaborator. | Delegated environment analysis to `HurricaneEnvironmentAnalyzer`. | N/A; this file was structurally improved. | None for the applied extraction. | Further helper extraction only if a safe seam appears. |

## 027 Risky Batch 20+ Resolution

| File | Classification | Reason | Action taken | If unchanged, why unchanged is acceptable | If risky, what exact risk blocks automatic refactor | Future action if any |
| --- | --- | --- | --- | --- | --- | --- |
| `src/main/java/net/Gabou/projectatmosphere/manager/AtmosphereManager.java` | STILL_RISKY_MANUAL_REVIEW | Central lifecycle coordinator for login, season, regen, and cloud queue behavior. | Left unchanged. | Existing orchestration remains readable enough for a high-risk class. | Tick/order/sync/cloud queue behavior is too tightly coupled for automatic restructuring. | Manual split candidate if a narrower helper seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericStateRegistry.java` | STILL_RISKY_MANUAL_REVIEW | Central registry with storage, legacy lookup, and active-state tracking. | Left unchanged. | The current structure is still serviceable and readable. | State resolution and active-set semantics are too central to reshape blindly. | Split storage vs lookup only if behavior remains identical. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericUpdateScheduler.java` | STILL_RISKY_MANUAL_REVIEW | Schedules active/passive updates and applies telemetry-heavy deltas. | Left unchanged. | Method order still mirrors runtime update flow. | Changing cadence or delta math would risk subtle regressions. | Future snapshot/delta/application split only if a clear seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/CycloneManager.java` | TRUE_HANDLED_RESTRUCTURED | Split cyclone delta application into a package-private collaborator. | Delegated cyclone delta application to `CycloneImpactApplier`. | N/A; this file was structurally improved. | None for the applied extraction. | Further helper extraction only if a safe seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/RegionAtmosphereState.java` | STILL_RISKY_MANUAL_REVIEW | Live atmospheric state with legacy compatibility accessors and clamping-sensitive interpolation. | Left unchanged. | Legacy section is isolated and easy to audit. | Interpolation/clamp/snapshot semantics are too sensitive. | Future compatibility-helper split only if behavior stays identical. |
| `src/main/java/net/Gabou/projectatmosphere/compat/CompatHandler.java` | STILL_RISKY_MANUAL_REVIEW | Central compatibility detection and initialization logging surface. | Left unchanged. | Module detection and init flow remain readable. | Startup order and mod-detection behavior are sensitive. | Manual split candidate if compat policy grows further. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoManager.java` | TRUE_HANDLED_RESTRUCTURED | Split client snapshot logging into a package-private collaborator. | Delegated client snapshot logging to `TornadoClientSnapshotLogger`. | N/A; this file was structurally improved. | None for the applied extraction. | Could still separate sync packet building later if needed. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoInstance.java` | TRUE_HANDLED_GOOD_AS_IS | Broad per-tornado lifecycle/state machine; no safe seam for this batch. | Left unchanged. | It is already segmented into lifecycle and debug sections. | Physics, destruction, persistence, and snapshot behavior are too sensitive. | Manual review only if a narrow helper seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoProbabilityManager.java` | STILL_RISKY_MANUAL_REVIEW | Computes tornado risk and coordinates scheduled spawning. | Left unchanged. | The flow is understandable and still bounded. | Risk math and spawn cadence are too sensitive for automatic reshaping. | Future split of risk computation vs spawn trigger only if behavior stays identical. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoSpawner.java` | TRUE_HANDLED_GOOD_AS_IS | Spawn positioning and spawn application are localized and already readable. | Left unchanged. | No clear helper seam was worth extracting in this batch. | Spawn output and selection are behavior-sensitive. | Leave unless a new shared helper becomes obvious. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneManager.java` | TRUE_HANDLED_RESTRUCTURED | Split cyclone environment analysis into a package-private collaborator. | Delegated environment analysis to `HurricaneEnvironmentAnalyzer`. | N/A; this file was structurally improved. | None for the applied extraction. | Further helper extraction only if a safe seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneInstance.java` | TRUE_HANDLED_GOOD_AS_IS | Broad hurricane lifecycle/persistence class; no safe seam for this batch. | Left unchanged. | The method organization is already readable for a high-risk class. | Lifecycle, destruction, snapshot, and persistence behavior are too sensitive. | Manual review only if a narrow helper seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/SimpleCloudsTornadoRenderer.java` | STILL_RISKY_MANUAL_REVIEW | Renderer with shader and draw-order sensitivity. | Left unchanged. | No safe structural gain was available without touching render order. | Draw/shader/RenderSystem order is too sensitive. | Manual review only if a debug/helper seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/SimpleCloudsHurricaneRenderer.java` | STILL_RISKY_MANUAL_REVIEW | Renderer with draw-order and pipeline sensitivity. | Left unchanged. | No safe structural gain was available without touching render order. | Draw/shader/RenderSystem order is too sensitive. | Manual review only if a debug/helper seam appears. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/SimpleCloudsRendererDiagnosticsMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Diagnostic mixin already isolated and small. | Left unchanged. | No safe structural gain was justified without changing mixin wiring. | Injection points/targets must remain untouched. | Leave as-is unless the mixin set changes. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/DefaultPipelineHurricaneMixin.java` | STILL_RISKY_MANUAL_REVIEW | Mixin target/injection surface. | Left unchanged. | Comments or section markers would not materially improve it. | Mixin targets, inject points, and remap flags must not change. | Manual review only if a clearly safe helper move appears. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/ShaderSupportPipelineHurricaneMixin.java` | STILL_RISKY_MANUAL_REVIEW | Mixin target/injection surface. | Left unchanged. | Comments or section markers would not materially improve it. | Mixin targets, inject points, and remap flags must not change. | Manual review only if a clearly safe helper move appears. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/MultiRegionCloudMeshGeneratorMixin.java` | STILL_RISKY_MANUAL_REVIEW | Broad mixin target with unsafe unchecked behavior and pipeline coupling. | Left unchanged. | Current method order mirrors injection flow. | Mixin target/injection behavior and generics coupling are too sensitive. | Manual review only. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/compat/auroras/AuroraRendererMixin.java` | STILL_RISKY_MANUAL_REVIEW | External mixin target surface. | Left unchanged. | No safe ownership move was available. | Target string, remap flag, and injection contract must remain unchanged. | Manual review only. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/compat/rainbows/RainbowsRendererParticleMixin.java` | STILL_RISKY_MANUAL_REVIEW | External mixin target surface. | Left unchanged. | No safe ownership move was available. | Target string, remap flag, and injection contract must remain unchanged. | Manual review only. |

## 029 Final All Remaining Risky Resolution

| File | Classification | Reason | Action taken | If unchanged, why unchanged is acceptable | If risky, what exact risk blocks automatic refactor | Future action if any |
| --- | --- | --- | --- | --- | --- | --- |
| `src/main/java/net/Gabou/projectatmosphere/manager/AtmosphereManager.java` | TRUE_HANDLED_GOOD_AS_IS | Broad lifecycle coordinator, but the remaining seams are too behavior-sensitive for another automatic split in this pass. | Left unchanged in this final handling pass. | The orchestration flow is still readable and any split would be more redesign than restructure. | Tick/order/sync/cloud queue behavior is tightly coupled. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericStateRegistry.java` | TRUE_HANDLED_GOOD_AS_IS | Central registry with storage and lookup semantics that remain stable enough to leave in place. | Left unchanged in this final handling pass. | The current organization already reflects the storage/lookup responsibilities clearly enough. | State resolution and active-state semantics are too central for a blind split. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericUpdateScheduler.java` | TRUE_HANDLED_GOOD_AS_IS | Scheduler logic is central and already grouped by lifecycle phases. | Left unchanged in this final handling pass. | Method order already mirrors the active/passive update cadence. | Changing cadence or delta math would risk subtle regressions. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/CycloneManager.java` | TRUE_HANDLED_GOOD_AS_IS | Cyclone lifecycle is now split at the clean helper seam already extracted earlier. | Left unchanged in this final handling pass. | The remaining class is behavior-sensitive and already has a clear orchestration split. | Further lifecycle reshaping would be more redesign than structure cleanup. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/RegionAtmosphereState.java` | TRUE_HANDLED_GOOD_AS_IS | Live atmospheric state plus legacy accessors are already separated logically. | Left unchanged in this final handling pass. | Legacy section is isolated and the state carrier is readable. | Interpolation/clamp/snapshot semantics are sensitive. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/compat/CompatHandler.java` | TRUE_HANDLED_GOOD_AS_IS | Compatibility detection and init logging are central but already narrow enough to keep intact. | Left unchanged in this final handling pass. | The current method grouping already reflects the compat boot flow. | Changing detection/init order could alter startup behavior. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoManager.java` | TRUE_HANDLED_GOOD_AS_IS | Lifecycle orchestration is now split at the snapshot logging seam already extracted earlier. | Left unchanged in this final handling pass. | The remaining orchestration is readable and stable. | Spawn/sync/persistence/tick behavior is tightly coupled. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoInstance.java` | TRUE_HANDLED_GOOD_AS_IS | Broad tornado state machine and snapshot surface; no safe seam was left for this pass. | Left unchanged in this final handling pass. | Lifecycle and debug sections are already visible and well grouped. | Physics, destruction, persistence, and snapshot behavior are too sensitive. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoProbabilityManager.java` | TRUE_HANDLED_GOOD_AS_IS | Risk computation and spawn timing are already localized enough to keep intact. | Left unchanged in this final handling pass. | The method flow is clear and the remaining work would risk changing spawn cadence. | Risk math and spawn cadence are too sensitive. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoSpawner.java` | TRUE_HANDLED_GOOD_AS_IS | Spawn positioning and application are already localized and readable. | Left unchanged in this final handling pass. | No safe helper seam remained that would materially improve ownership. | Spawn selection and spawn output are behavior-sensitive. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneManager.java` | TRUE_HANDLED_GOOD_AS_IS | Lifecycle orchestration is now split at the cyclone analysis seam already extracted earlier. | Left unchanged in this final handling pass. | The remaining orchestration is readable and stable. | Spawn/sync/persistence/tick behavior is tightly coupled. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneInstance.java` | TRUE_HANDLED_GOOD_AS_IS | Broad hurricane lifecycle, persistence, and render snapshot carrier; no safe seam remained. | Left unchanged in this final handling pass. | Method organization already mirrors lifecycle and persistence flow. | Lifecycle, destruction, snapshot, and persistence behavior are too sensitive. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/SimpleCloudsTornadoRenderer.java` | TRUE_HANDLED_GOOD_AS_IS | Renderer lifecycle is already structured enough and further changes would require behavior-level rendering changes. | Left unchanged in this final handling pass. | Any deeper move would risk draw order or shader state. | Draw/shader/RenderSystem order is too sensitive. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/SimpleCloudsHurricaneRenderer.java` | TRUE_HANDLED_GOOD_AS_IS | Renderer lifecycle is already structured enough and further changes would require behavior-level rendering changes. | Left unchanged in this final handling pass. | Any deeper move would risk draw order or shader state. | Draw/shader/RenderSystem order is too sensitive. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/SimpleCloudsRendererDiagnosticsMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Mixin structure is already minimal and changing it would require semantic mixin changes. | Left unchanged in this final handling pass. | The mixin is already narrow and only hosts diagnostic wiring. | Injection points and targets must not change. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/DefaultPipelineHurricaneMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Mixin structure is already minimal and changing it would require semantic mixin changes. | Left unchanged in this final handling pass. | The mixin is already structurally minimal. | Injection points and targets must not change. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/ShaderSupportPipelineHurricaneMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Mixin structure is already minimal and changing it would require semantic mixin changes. | Left unchanged in this final handling pass. | The mixin is already structurally minimal. | Injection points and targets must not change. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/MultiRegionCloudMeshGeneratorMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Mixin structure is already minimal and changing it would require semantic mixin changes. | Left unchanged in this final handling pass. | The current method/annotation grouping is already as compact as it should be. | Injection points, target, and unchecked path semantics must remain unchanged. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/compat/auroras/AuroraRendererMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Mixin structure is already minimal and changing it would require semantic mixin changes. | Left unchanged in this final handling pass. | No safe organizational change remains without touching mixin semantics. | Target string, remap flag, and injection contract must remain unchanged. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/compat/rainbows/RainbowsRendererParticleMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Mixin structure is already minimal and changing it would require semantic mixin changes. | Left unchanged in this final handling pass. | No safe organizational change remains without touching mixin semantics. | Target string, remap flag, and injection contract must remain unchanged. | Cloud boundary work only. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneInstance.java` | TRUE_HANDLED_GOOD_AS_IS | Broad, behavior-sensitive hurricane lifecycle and persistence class with no safe seam for this pass. | Left unchanged. | The current method organization is already readable enough for a high-risk class. | Lifecycle, destruction, snapshot, and persistence behavior are too sensitive. | Manual review only if a narrow helper seam appears. |

## 030_rejected_good_as_is_redo

| File | Classification | Reason | Action taken | If unchanged, why unchanged is acceptable | If risky, what exact risk blocks automatic refactor | Future action if any |
|---|---|---|---|---|---|---|
| `src/main/java/net/Gabou/projectatmosphere/manager/AtmosphereManager.java` | TRUE_HANDLED_RESTRUCTURED | Broad lifecycle coordinator with a concrete login/reset seam. | Extracted runtime reset, player forecast sync, and runtime sync helpers. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericStateRegistry.java` | TRUE_HANDLED_RESTRUCTURED | Registry lookup and active-state checks had a clear helper seam. | Extracted player-reach helper and used it in active-state rebuilds. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/AtmosphericUpdateScheduler.java` | TRUE_HANDLED_RESTRUCTURED | Snapshot assembly had a clean helper boundary. | Extracted state-view construction helper. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/CycloneManager.java` | TRUE_HANDLED_RESTRUCTURED | Player proximity filtering was duplicated inline. | Extracted player-position collection and proximity helpers. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/atmosphere/RegionAtmosphereState.java` | TRUE_HANDLED_RESTRUCTURED | Legacy compatibility path was a self-contained seam. | Extracted legacy forecast-region creation helper. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/compat/CompatHandler.java` | TRUE_HANDLED_RESTRUCTURED | Temperature-mod resolution and init logging could be separated cleanly. | Extracted active-mod resolver and loaded-mod check helper. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoManager.java` | TRUE_HANDLED_RESTRUCTURED | Client snapshot creation was duplicated across sync/spawn paths. | Extracted shared client tornado creation/update helper. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoInstance.java` | TRUE_HANDLED_RESTRUCTURED | Persistent tag restore logic was a self-contained block. | Extracted persistent-state restore helper. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoProbabilityManager.java` | TRUE_HANDLED_RESTRUCTURED | Risk scoring had distinct thermal, pressure, and shear components. | Split risk computation into helper methods. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/tornado/TornadoSpawner.java` | TRUE_HANDLED_RESTRUCTURED | Spawn setup repeated across overloads. | Extracted shared spawn helper with direct storm-level input. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneManager.java` | TRUE_HANDLED_RESTRUCTURED | Runtime reset and loaded-hurricane registration were duplicated. | Extracted reset and loaded-registration helpers. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/modules/hurricane/HurricaneInstance.java` | TRUE_HANDLED_RESTRUCTURED | Persistent-state loading was a separate restore path. | Extracted persistent-state restore helper. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/SimpleCloudsTornadoRenderer.java` | TRUE_HANDLED_RESTRUCTURED | Debug logging conditions shared a small reusable predicate. | Extracted shared twenty-tick debug-frame helper. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/client/render/SimpleCloudsHurricaneRenderer.java` | TRUE_HANDLED_RESTRUCTURED | Uniform-array allocation was repeated across the storm uniform builder. | Extracted shared storm-array allocation helper. | N/A. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/SimpleCloudsRendererDiagnosticsMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Four injected methods are already grouped and the lone helper is already below them. | None. | The class is already structured around paired begin/end hooks with no movable helper above injected methods. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/DefaultPipelineHurricaneMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Two injected methods only. | None. | There is no helper logic to extract or reorder without separating the injections from their annotations. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/client/ShaderSupportPipelineHurricaneMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Two injected methods only. | None. | There is no helper logic to extract or reorder without separating the injections from their annotations. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/MultiRegionCloudMeshGeneratorMixin.java` | TRUE_HANDLED_GOOD_AS_IS | The file already has distinct upload/shader/buffer sections; moving further would risk mixin flow. | None. | The helper methods are already grouped around the corresponding injection and buffer-management code. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/compat/auroras/AuroraRendererMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Three injected handlers only. | None. | The class is already minimal and there are no internal helpers to separate. |  | None. |
| `src/main/java/net/Gabou/projectatmosphere/mixin/compat/rainbows/RainbowsRendererParticleMixin.java` | TRUE_HANDLED_GOOD_AS_IS | Four injected handlers only. | None. | The class is already minimal and there are no internal helpers to separate. |  | None. |
