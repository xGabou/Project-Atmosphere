# Proposed Future Classes

This folder intentionally contains **notes only**. No real source code is being added here.

## Future-only concepts to consider later

- `CloudRenderSnapshot`
- `CloudRenderStateCache`
- `CloudLightingSnapshot`
- `CloudShadowSnapshot`
- `CloudRenderController`

## Purpose

These are not implementation instructions. They are placeholders for a future architecture discussion so the renderer can consume PA-owned cloud data without reaching back into simulation classes.

